# Fire & Rescue Services campaign page

Copy this whole folder into the published root of your existing GitHub Pages repository.

The subpage will resolve at `/fire-rescue-services/`.

If GitHub Pages publishes from `/docs`, place this folder inside `/docs` instead.

No build step is required. All page assets use relative paths.
